{
  "name": "anaqa-web",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && (cd client && node node_modules/vite/bin/vite.js build) && npm install --prefix server",
    "start": "node server/server.js",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=22"
  }
}
